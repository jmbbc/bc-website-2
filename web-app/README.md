# Web App (Full React)

PoC React app for the BC website.

Run locally:

1. cd web-app
2. npm install
3. npm run dev -- --host

Notes:
- Copy assets (images) into `web-app/public/assets/` or update paths in `src/pages/Notices.jsx`.
- This is a starting point; I will add routes and convert other pages next.
